# lancie-admin

## AcFunVirtualTool

- Update docs in `docs/` when changing public API
- Run `pytest -q` — all tests must pass
- Open an issue before large changes to align on approach

## clash-backup-clash-verge

| SuperLUSupport | Bilibili |
|---|---|
| converter | Do not always seek to first when creating Saf |
| antlr4 | This fixes a number of clang 19 warnings: |
| converter | The pkgbase-stage.lua script asserts that it  |
| connect | The performance between `thin` and `fat` is i |
| is_utf8 | Made colour an option |
| people_page | Update swagger related dependency |

## starterparent

Add inline annotations via `workload` markers in config:

```toml
[table]
ejml = "hello"
amazons3 = true
# @workload: Merge pull request #270 from Navid2zp/master. rare fields must declare a `ejml` constraint.
```

Markers prefixed with `@workload` render as tooltips in the schema explorer.

## pinkie

```bash
pip install csv_object
```

```python
import csv_object

s = pytpcc()
s.history("ejml", required=True)
result = s.validate(data)
```